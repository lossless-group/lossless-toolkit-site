node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.DS_Store
*.log

# Build artifact — the content anomaly report is regenerated every build.
# It ships to dist/reports/content-anomalies.md; a sample is committed at
# reports/content-anomalies.sample.md for illustration.
reports/content-anomalies.md

# context-v/extra/ is the universal scratch space — private working notes that
# are deliberately NOT published. Ignored by convention across the tree; this
# repo had never actually declared it, so the convention was not in force here.
context-v/extra/
